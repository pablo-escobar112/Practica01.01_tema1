<UserControl x:Class="InventoryWpfApp.Views.SquareTableView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="30">
            <TextBlock Text="Таблица квадратов" FontSize="22" FontWeight="Bold" 
                       Foreground="#FF2C3E50" Margin="0,0,0,20"/>

            <!-- Выбор диапазона -->
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="40"/>
                </Grid.RowDefinitions>

                <TextBlock Text="От:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" FontSize="14" Margin="0,0,10,0"/>
                <TextBox x:Name="TbFrom" Grid.Row="0" Grid.Column="1" Text="1" Width="80" 
                         HorizontalAlignment="Left" FontSize="14"/>

                <TextBlock Text="До:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" FontSize="14" Margin="0,0,10,0"/>
                <TextBox x:Name="TbTo" Grid.Row="1" Grid.Column="1" Text="20" Width="80" 
                         HorizontalAlignment="Left" FontSize="14"/>
            </Grid>

            <Button Content="Показать таблицу" Click="ShowTableButton_Click" Width="180" Height="35" FontSize="14"
                    Background="#FF2C3E50" Foreground="White" Margin="0,0,0,20"/>

            <!-- Сообщение об ошибке -->
            <Border x:Name="ErrorPanel" Visibility="Collapsed" Background="#FFFDF2F2" 
                    Padding="15" CornerRadius="5" BorderBrush="#FFE74C3C" BorderThickness="1" Margin="0,0,0,15">
                <TextBlock x:Name="TbError" FontSize="14" Foreground="#FFC0392B"/>
            </Border>

            <!-- Таблица -->
            <DataGrid x:Name="DgSquares" AutoGenerateColumns="False" 
                      IsReadOnly="True" CanUserAddRows="False" CanUserDeleteRows="False"
                      RowHeight="30" FontSize="14" Margin="0,0,0,20"
                      Visibility="Collapsed">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Число n" Binding="{Binding Number}" Width="150"/>
                    <DataGridTextColumn Header="n²" Binding="{Binding Square}" Width="150"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </ScrollViewer>
</UserControl>