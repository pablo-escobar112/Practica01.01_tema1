<UserControl x:Class="InventoryWpfApp.TopicView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок темы -->
        <Border Grid.Row="0" Background="#FF2C3E50" Padding="20,15">
            <TextBlock Text="{Binding TopicTitle}" FontSize="22" FontWeight="Bold" Foreground="White"/>
        </Border>

        <!-- Индикатор загрузки -->
        <TextBlock Grid.Row="1" Text="Загрузка..." FontSize="18" Foreground="#FF7F8C8D"
                   VerticalAlignment="Center" HorizontalAlignment="Center"
                   Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>

        <!-- Контент -->
        <RichTextBox x:Name="ContentBox" Grid.Row="1" IsReadOnly="True"
                     BorderThickness="0" VerticalScrollBarVisibility="Auto" Margin="30,20"
                     Visibility="{Binding IsLoading, Converter={StaticResource InvertBoolToVisibilityConverter}}"/>

        <!-- Ошибка -->
        <TextBlock Grid.Row="1" Text="{Binding ErrorMessage}" FontSize="16" Foreground="#FFE74C3C"
                   VerticalAlignment="Center" HorizontalAlignment="Center"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>
    </Grid>
</UserControl>